import React from 'react'

const BigEventCard = (props) => {
  return (
    <div>

        <h1>This is the big event card for event number {props.param}</h1>
      
    </div>
  )
}

export default BigEventCard
